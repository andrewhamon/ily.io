@keyframes fade-in
  from { opacity: 0 }
  to   { opacity: 1 }
  
@keyframes fade-to-black
  from {  }
  to   { color: #000000 }

@keyframes zoom-out-heart
  from {  }
  1%   { transform: scale(10) }
  to   { transform: scale(1)  }
